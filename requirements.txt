# =============================================================================
#  BetDoc — Production Runtime Dependencies
#  Target interpreter: CPython 3.12+ (3.11 is the hard floor)
#  Pins are EXACT by policy. Bump deliberately; never widen to an open range.
#
#    python -m venv .venv
#    .venv\Scripts\activate          # Windows
#    pip install -r requirements.txt
# =============================================================================

# ---- Pillar 1: Ingestion — transport & session layer ------------------------
aiohttp==3.14.3                 # async HTTP pooling for REST poll adapters
httpx==0.28.1                   # HTTP/2 client, used by typed API adapters
websockets==17.1                # raw WS framing for push feeds
tenacity==9.1.4                 # backoff / retry policy on reconnect storms
playwright==1.62.0              # authorized UI automation only (see ingestion/README.md)

# ---- Pillar 2: Quantitative engine -----------------------------------------
numpy==2.5.2                    # scoreline matrices, vectorised Kelly
scipy==1.18.1                   # optimize (Kelly), stats (Poisson, MVN copula)
pandas==3.0.5                   # historical frames, CLV panels
statsmodels==0.15.0             # Dixon-Coles fitting, regression diagnostics

# ---- Pillar 4 + persistence: state & history -------------------------------
redis==8.1.0                    # millisecond live-odds state (async client)
asyncpg==0.31.0                 # PostgreSQL driver, no ORM overhead on hot path
SQLAlchemy==2.0.52              # typed 2.0 ORM for historical / CLV models
alembic==1.19.1                 # schema migrations

# ---- API surface & configuration -------------------------------------------
fastapi==0.141.1                # internal monitoring dashboard backend
uvicorn[standard]==0.52.4       # ASGI server (uvloop + httptools)
pydantic==2.13.5                # validation at system boundaries
pydantic-settings==2.15.0       # typed settings, env overlay
PyYAML==6.0.3                   # config.yaml parsing
orjson==3.12.0                  # fast serialisation for Redis payloads

# ---- Scheduling & observability --------------------------------------------
APScheduler==3.11.3             # settlement sweeps, closing-line capture jobs
structlog==26.1.0               # structured JSON logs, correlation ids
prometheus-client==0.26.0       # latency / edge / exposure metrics
