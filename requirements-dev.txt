# =============================================================================
#  BetDoc — Development & CI Dependencies
#    pip install -r requirements.txt -r requirements-dev.txt
# =============================================================================

-r requirements.txt

# ---- Test suite -------------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
hypothesis==6.167.1             # property tests: probabilities must always sum to 1
freezegun==1.5.5                # deterministic clocks for latency / CLV tests
respx==0.23.1                   # mock httpx transports for adapter tests

# ---- Static analysis --------------------------------------------------------
mypy==2.3.1                     # strict mode, no untyped defs
ruff==0.16.5                    # lint + format
types-PyYAML==6.0.12.20260815
